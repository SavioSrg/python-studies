print("---------  Cadastro para IMC ---------")
nome = input("Qual é o nome? ")
idade =int(input("QUal é a sua idade? "))
peso = float(input("Qual é a seu peso atual (em quilogramas)? "))
altura = float(input("Qual é a sua altura? "))

imc = peso / (altura**2)

print("---------  Ficha do paciente ---------")
print ("TIpo de variável nome:", nome, type(nome))
print ("TIpo de variável idade:", idade, type(idade))
print ("TIpo de variável peso:", peso, type(peso))
print ("TIpo de variável altura:", altura, type(altura) )

print("---------  Calculo IMC ---------")
print(nome, ", o cálculo utilizado para relacionar peso e altura é o Índice de Massa Corporal (IMC), que serve para avaliar o estado nutricional de um indivíduo. O nosso calculo obteve os seguinte resultado:\n IMC:", imc)